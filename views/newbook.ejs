<header class="site-header d-flex flex-column justify-content-center align-items-center">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h2 class="mb-0">New Book</h2>
            </div>
        </div>
    </div>
</header>
<section class="about-section" id="section_2">
    <div class="container">
        <div class="row">
            <div class="col-md-12 justify-content-center align-items-center d-flex flex-column">
                        <h4 class="col-lg-12 text-center">Add Book</h4>
                        <div class="subscribe-form-wrap">
                            <form class="custom-form subscribe-form" action="/logs" method="POST" enctype="multipart/form-data">
                                <label for="title">Title</label>
                                <input type="text" name="title" class="form-control"><br>
                                <label for="title">Tag</label>
                                <select name="tag">
                                    <% tags.forEach(tag => { %>
                                        <% if (tag.id === book.tag) { %>
                                            <option selected label="<%= tag.name %>" value="<%= tag.id %>"></option>
                                        <% } else { %>
                                            <option label="<%= tag.name %>" value="<%= tag.id %>"></option>
                                            <% } %>
                                    <% }) %>
                                </select>
                                <label for="snippet">Description</label>
                                <input type="text" name="description" class="form-control"><br>
                                <label for="author">Author</label>
                                <input type="text" name="author" class="form-control"><br>
                                <label for="authorProfession">Number of Pages</label>
                                <input type="number" name="numberofpages" class="form-control"><br>
                                <label for="body">Image</label>
                                <textarea type="file" name="image" class="form-control"></textarea>
                                <div class="col-lg-12 col-12">
                                    <button type="submit" class="form-control btn btn-success">Add Book</button>
                                </div>
                            </form>
                        </div>
                </div>
            </div>
        </div>
    </div>
</section>
